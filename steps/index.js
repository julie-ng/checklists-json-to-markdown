const createIndexFiles = require('./create-category-index-files')
const createItemFiles = require('./create-item-files')
const setup = require('./setup')

module.exports = {
  createIndexFiles,
  createItemFiles,
  setup
}